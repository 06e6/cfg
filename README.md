## Config

```lua
getgenv().Config = {
    USERNAME_TO_RECEIVE_ITEMS = "",
    RARITY_OF_ITEMS = "", -- Common, Uncommon, Rare, Legendary, Godly, Ancient
    SERVER_INFO_WEBHOOK = "",
    DISCORD_ID = "",
    PLAYER_LOAD_DELAY = "10", -- waiting time for receiving account
    TRANSFER_FROM_MOBILE = true -- set to true if you're transferring from mobile
}
```

